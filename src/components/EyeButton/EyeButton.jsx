import React from 'react';
import PropTypes from 'prop-types';

EyeButton.propTypes = {
    
};

const EyeButton = ({initialModalAction}) => {

    return (
        <button><i class="far fa-eye"></i></button>
    );
}

export default EyeButton;